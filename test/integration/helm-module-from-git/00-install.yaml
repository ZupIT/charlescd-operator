apiVersion: deploy.charlescd.io/v1alpha1
kind: Module
metadata:
  name: charlescd
spec:
  helm:
    gitRepository:
      path: install/helm-chart
      ref:
        type: tag
        value: 1.0.1
      url: https://github.com/ZupIT/charlescd
