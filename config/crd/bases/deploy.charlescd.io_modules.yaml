
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: modules.deploy.charlescd.io
spec:
  group: deploy.charlescd.io
  names:
    kind: Module
    listKind: ModuleList
    plural: modules
    singular: module
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Module is the Schema for the modules API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ModuleSpec defines the desired state of Module
            properties:
              repository:
                description: Repository defines the location where sources are stored
                properties:
                  git:
                    description: Git defines the address where sources are tracked
                    properties:
                      path:
                        type: string
                      ref:
                        description: Ref defines references to a specific commit
                        properties:
                          branch:
                            type: string
                          commit:
                            type: string
                          tag:
                            type: string
                        type: object
                      url:
                        type: string
                    type: object
                  helm:
                    description: Helm defines the address where charts are packaged
                    properties:
                      chart:
                        type: string
                      url:
                        type: string
                      version:
                        type: string
                    type: object
                type: object
              values:
                type: object
            required:
            - repository
            type: object
          status:
            description: ModuleStatus defines the observed state of Module
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
